{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Robot Panel",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/robot_operator_panel",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "CMake Build",
            "miDebuggerPath": "/usr/bin/gdb"
        }
    ]
}
